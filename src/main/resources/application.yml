spring:
  config:
    import:
    

  application:
    name: event-management
  datasource:
    url: jdbc:postgresql://localhost:5432/event_sales
    username: dev 
    password: dev
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    locations: classpath:/db/migration
    baseline-on-migrate: false
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080